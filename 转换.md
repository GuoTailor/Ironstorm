Mathf.angle -> MathUtils.atan2Deg360 注意x,y 参数是反的
Mathf.atan2 -> MathUtils.atan2 注意x,y 参数是反的
Mathf.slerp -> MathUtils.lerpAngleDeg
Rect -> Rectangle
Vec2 -> Vector2
Geometry -> GeometryUtils

以下为最小实现补充（2026/9 编译修复）：
Mathf.clamp/lerp/lerpDelta/slerp/slerpDelta/random/chance/absin/range -> 已补进 com.phoenix.game.math.Mathf
Tmp.v1.trns(angle, len) -> Tmp.v1.set(len, 0).rotateDeg(angle) 注意 Tmp.v3 是 Vector3 不是 Vector2
Rectangle 没有 overlaps(x,y,w,h) 重载，需要手写：r.x < x+w && r.x+r.width > x && r.y < y+h && r.y+r.height > y
Sound.at(x,y) -> libgdx Sound 无 .at()，先直接 play(1f)
arc 的 Scaled（fin()）、DrawTrait（draw()）接口未移植前，实现类上不要加 @Override
Effects.Effect -> com.phoenix.game.entities.Effects.Effect（嵌套类，含 EffectContainer/EffectRenderer）
Fx.none 等特效 -> com.phoenix.game.content.Fx（占位实现）
Team -> com.phoenix.game.core.Team（最小实现，isEnemy 简化为 other != this）
@Remote 注解（网络同步）未移植前要注释掉，否则注解处理器报 "No @WriteClass method" 导致编译中断

世界系统/战役模式补充（2026/9）：
Angles.angle(x,y,x2,y2) 必须写成 MathUtils.atan2(y2-y, x2-x)，参数反了会让瞄准/朝向整体偏 90°
libgdx 的 Array for-each 迭代器不能嵌套，热路径（Units/Damage/Effects/Renderer/Logic）统一用下标循环
Bullet.remove() 会放回对象池并清空 type，命中回调里要先把 type 存成局部变量再使用
实体碰撞：EntityCollisions.collideGroups(外层, 内层)，外层尽量放不会被回收的实体（子弹放内层）
单位死亡要先 setDead(true) 再触发爆炸伤害，否则爆炸会递归触发 onDeath 直到栈溢出
Tile/Block/Floor/Blocks/World 放在 com.phoenix.game.world，状态效果放在 com.phoenix.game.type

窗口 / 相机 / 输入（与 Mindustry 桌面端对齐，2026/9）：
窗口：默认 900x700 + 启动最大化，depth/stencil 都为 0（对应原版 SdlConfig 的 width/height/maximized/depth/stencil）
相机缩放：Renderer 里 targetscale / camerascale，默认 Scl.scl(4f)，逐帧 Mathf.lerpDelta(camerascale, targetscale, 0.1f)，
         范围 [Scl.scl(1.5f), Math.round(Scl.scl(6))]，最后 camera.zoom = camerascale（libgdx 的 zoom 与 arc 的 camera.width = 屏幕宽/scale 等价）
输入：input/InputHandler + input/DesktopInput，arc 的 Core.input 用 Gdx.input 替代；
     WASD/方向键平移（Shift 加速 3f→8f）、鼠标侧键(Buttons.BACK)朝光标平移、滚轮缩放、F11 全屏、空格暂停
注意：Scl.scl() 不能在类初始化阶段调用（那时 Core.app 还是 null），相机缩放要懒初始化，否则 clamp 会把 -1 夹到下限 1.5
注意：libgdx 的 Vector3 没有 add(Vector2, float)，相机平移用 position.x += / position.y +=
注意：arc 的 mouseY() 以屏幕下边缘为原点，libgdx 的 Gdx.input.getY() 以上边缘为原点，要写成 graphics.getHeight() - getY()
注意：libgdx 的 OrthographicCamera.zoom 与 arc 的 camerascale 语义相反（zoom 是缩小倍数），要写 camera.zoom = 1f / camerascale，
     否则地图会被缩成屏幕中间一小块
注意：ScreenViewport.update(w, h, true) 会把相机重新居中到屏幕中心，覆盖掉 Control.play() 设置的出生点视角
     （表现为地图偏移 + 大量黑边），resize() 里必须传 false
注意：世界贴图是按 4 倍分辨率导出的（grass 8 单位 -> 32px），所以必须像原版 Control.loadAsync() 那样设置
     Draw.scl = 1f / Core.atlas.find("scale_marker").getWidth()（scale_marker 是 4px -> 0.25），
     否则按贴图原始尺寸绘制的对象（单位、武器、腿部）会大 4 倍；地块/子弹用的是显式宽高，不受影响
动画相位（如走路的 walkTime）只能在 update() 里依据「本帧是否真的移动」推进，不能放 draw() 里无条件累加，
     否则单位静止时腿也一直乱动；baseRotation 由移动方向驱动，不要在 draw() 里用绝对坐标去插值
arc.struct 里 libgdx 没有的类型要自己补最小实现：IntQueue -> com.phoenix.game.struct.IntQueue（保持 size 公开字段/addFirst/removeLast 语义）
Vec2.angle() 在 arc 里返回**弧度**，libgdx 的 Vector2.angle() 返回**角度**，混用会让朝向错乱（单位 rotation 是角度制）
玩家单位控制：原版 Player 自身继承 Unit（带整套 Mech 系统），移植时简化为 Player 持有被控单位引用 + BaseUnit.isPlayer 跳过 AI；
     相机跟随写在 Renderer.update 里（camera.position.lerp(player, 0.08f)），死亡/暂停时才允许 WASD 平移相机

波次/炮塔：
子弹必须带队伍：原版 Bullet.create(type, owner, team, ...) 显式传 team，本项目改成默认继承发射者 + 提供带 team 的重载；
     否则炮塔子弹会把自家单位/建筑当敌人打（BaseUnit.collides 里对 Bullet 也要判队伍）
TileEntity.init 里要同步 tile.getTeam()：原版靠 create(tile) 传入队伍，否则实体队伍永远是默认阵营
建筑实体坐标是瓦片**左下角**（tile.worldx()），画中心/算索敌距离要 + tilesize/2f
地形瓦片（含边界墙）没有队伍（默认 derelict）也**没有实体**，玩家放置的方块才有队伍；本项目规则：友方实体/墙不阻挡也不吃伤害
原版 Time.run 是有任务队列的，本项目早先只是空实现（导致武器 shotDelay 连发完全失效），已补 run/clear + 每帧倒序推进
Time.delta 是**方法**不是字段：arc 的 Time 里根本没有 delta 字段（它只是 update() 里的局部变量，updateGlobal() 只推进 globalTime），
     所以一律写 Time.delta()。本项目曾多出一个 public static float delta 字段，恒为初始值 1f 且全工程无人赋值，
     读它的地方全部静默失效——传送带/钻头/合成器按 delta=1 推进（高帧率下偏快约 1.5 倍）、单位转向被 clamp(rotatespeed*1,0,1)
     钳到 1 而瞬间贴向目标、自动存档按帧而非按时间计时。该字段已删除。原版实体级写法是 TileEntity.delta() = Time.delta() * timeScale，
     本项目实体没有 timeScale，直接用 Time.delta()
原版炮塔逻辑写在 Block.update(tile) 里，本项目统一写在实体内部类 update()（与 Drill/GenericCrafter 一致）
炮塔贴图：原版 region=炮管、baseRegion="block-"+size；本项目 Renderer 平铺绘制 block.region，所以反过来：
     region = block-<size>（底座），炮管存 barrelRegion 在 drawLayer 里按 entity.rotation 旋转绘制
波次出生点：原版来自地图 tile 的 spawn overlay（OverlayFloor）；本项目在 World.createMap 四角放置该 overlay，WaveSpawner 扫描得到出生点

HUD 缩放：Stage 不能复用世界相机的 viewport（原工程 `new ScreenViewport(Core.camera)` + `new Stage(viewport)`），
     否则滚轮缩放世界时 HUD 一起放大；要给 Stage 单独的 ScreenViewport + 独立 OrthographicCamera，并在 resize 里一起 update

单位工厂：
EntityCollisions 有同样的"约定"坑：原版瓦片是**中心约定**（tile(x,y) 的中心在 x*8,y*8），本项目是**角点约定**（覆盖 [n*8,(n+1)*8)），
     所以 moveDelta/overlapsTile 里的 Math.round(...) 与 setCenter(wx*tilesize, wy*tilesize) 必须改成 toTile + 中心 +tilesize/2，
     否则单位会被挤进墙里半格并原地卡死（症状：出厂单位"卡在建筑里"）
Blocks 早于 UnitTypes 加载：方块要引用单位类型时用名字延迟解析（UnitTypes.all 里按 name 找）
多格贴图完整性：地板与方块必须**分两遍画**（对应原版 Renderer 的 drawFloor/drawBlocks）——
     多格贴图由中心格绘制、会延伸到"上方/右侧"的格子，若按每格"地板+方块"顺序画，上面那几格的地板会后画，
     把贴图上半部分盖掉（症状：贴图缺上面一半/三分之一）
多格贴图：Renderer 过滤卫星瓦片要用 `tile.isLinked()`（`block instanceof BlockPart`），**不能**用 `tile.block().isHidden()`——
     `tile.block()` 对卫星瓦片会转发到中心方块，卫星的 isHidden() 恒为 false，结果每个格子各画一遍整张贴图
     （3x3 核心会显示成 9 个重复副本，2x2 工厂显示成 4 个）
多格建筑现在奇偶尺寸都支持：footprint 用原版写法「锚点 + 0..size-1 + (-(size-1)/2)」（奇数关于锚点对称，偶数时锚点在最靠近原点的角），
     并且 Block.offset() = ((size+1)%2)*tilesize/2（偶数偏半格）—— 原点约定下偶数建筑的几何中心落在两格交界处，渲染/中心计算都必须带上这个偏移
     （旧的 (size-1)/2 对称循环让偶数尺寸退化成 1x1：贴图盖 2x2、逻辑只占 1 格）
追击移动目标走直线时，被障碍挡住的单位需要"卡住一定时间改用流场"的回退，否则会永远顶着建筑

传送带 / 方块朝向：
贴图名是「名称-拼接组-帧」（图集里只有 conveyor-0-0 ~ conveyor-4-3，共 5 组 × 4 帧，**没有**名为 conveyor 的单图），
     走父类 loadRegions 会找不到而退回 blank（症状：传送带是一条白色方块；storage 同理，图集里叫 container）
绘制带朝向的贴图用 batch.draw(region, x, y, originX, originY, w, h, scaleX, scaleY, rotation)：
     原点(+半宽半高) + 角度，负的 scaleY 即"镜像"（在贴图自身坐标系里翻转，与原版 blendsclY = -1 等价）
贴图基准朝向是**右**（原版 Conveyor.draw 用 rotation*90，其 rotation 0 = +x）；本项目 rotation 0 = +y（与 Geometry.d4 一致），
     所以角度 = (1 - rotation) * 90
拼接：照搬原版 Autotiler.buildBlending + Conveyor.transformCase 的索引运算（d4[Mathf.mod(rotation - direction, 4)]）——
     两边 d4 的元素顺序不同（原版从 +x 起，本项目从 +y 起），但公式是纯索引旋转，得到的拼接组/镜像完全一致
方块本体绘制从 Renderer 移进 Block.draw(Tile)（对应原版），带朝向/动画的方块覆盖它自己画；Block.rotate 决定放置时能否用 R 旋转
放置预览（对应原版 DesktopInput.drawTop）：半透明 block.region（按朝向旋转）+ 朝向箭头，绿=可放/红=不可放；
     注意 project() 得到的屏幕 Y 与 Gdx.input.getY() 同为「上边缘为 0」

连线放置（拖拽连放）：
原版 `InputHandler.iterateLine` 生成路径 —— 默认 `Placement.normalizeLine`（单轴直线），按住 Ctrl（`Binding.diagonal_placement`）才走
     `Placement.pathfindLine`（对传送带会 A* 寻路，带"转弯惩罚"）；逐格朝向 = 指向路径下一格，最后一格用"起点→终点"的 `baseRotation`
本项目取反：**可旋转方块（传送带）默认走 L 形路径**（先长轴后短轴，效果等价于原版带转弯惩罚的路径），按 Ctrl 强制直线
原版是"按下 → 拖拽生成 lineRequests 预览 → 松手 flushRequests 才建造"，本项目照此实现（不是边拖边建）
拖拽拆除：原版是矩形框选（drawBreakSelection），本项目简化为沿路径逐格拆除（用同一套 buildPath）

建造系统：
World.toTile 必须是**向下取整**（原工程用了 Mathf.round，瓦片中心 8n+4 会被算到 n+1 格），
     症状很有迷惑性：3x3 核心够大所以子弹/放置"看起来正常"，1x1 墙体却完全打不中、放置也偏半格
原版 Block.hasEntity 只看 update，墙体靠 TileEntity 承载血量；本项目改为 update || destructible，墙体用 Wall 类挂实体
原版子弹只与实心瓦片碰撞（传送带等非实心建筑不阻挡），且只有敌方建筑扣血 —— 友方墙会挡子弹但不掉血
World.ltile 必须解析多格链接（原版 = tile.block().linked(tile)，本项目 = tile.link()）：
     卫星瓦片（BlockPart）没有实体（entity == null），直接用 World.tile 会让「tile.entity != null」的判定全部落空
     症状：3x3 核心只有正中那一格能被子弹打到（即只有沿中心行/列飞来的子弹有效），能顶着十几波几乎不掉血
建造材料：原版走 Requirements + 核心库存/建造单位扣料，本项目简化为直接从队伍共享库存（Teams.items）扣与返还
     （返还比例对应 rules.deconstructRefundMultiplier，默认 0.5）

小地图（照搬 graphics/MinimapRenderer + io/MapIO + ui/Minimap + ui/fragments/MinimapFragment）：
原版 Block.createIcons() 在打包期取图标中心像素当小地图色；本项目没有打包管线，改在 Blocks.load() 末尾
     （Blocks.loadMinimapColors → Block.loadMinimapColor）运行期做一次
分页 PNG 从**已加载的图集纹理**反查：Texture.getTextureData() instanceof FileTextureData → getFileHandle() → new Pixmap(fh)；
     不要用 consumePixmap()（它要求先 prepare()，纹理加载完必抛 GdxRuntimeException）；
     也**不能**按 block.name 去图集查区域 —— Conveyor.load() 把 region 覆盖成了 conveyor-0-0，必须用 block.region 自身坐标
AtlasRegion 的 (x, y) 与 Pixmap.getPixel 同为「左上原点」，直接取 region 中心像素即可
headless 服务端的 Core.atlas 是**空 TextureAtlas**（ServerControl 塞的），不是 null；守卫要判 getRegions().size / region == null，
     否则会去读不存在的 sprites/sprites.atlas
Scene2D 里子 Actor 的 draw(batch, alpha) 用的是「父 Group 局部坐标」，自己的 getX()/getY() 要自己加上（Label.draw 就是这么写的）
Stage.scrolled 只发给 scrollFocus（**不做 hit 判定**，scrollFocus 为 null 时发给 root），
     要让某个 Actor 吃到滚轮必须显式 stage.setScrollFocus(actor)，否则事件会漏给 InputMultiplexer 后面的游戏输入
libgdx 的 Texture 只有整张上传的 draw(Pixmap, x, y)，没有 arc 的 Pixmaps.drawPixel（单像素写纹理）→
     用脏标记把同一帧的多次瓦片变化合并成一次上传
Actor.setSize 要两个参数（arc 有一个参数的版本）；Pools.obtain 没有 (Class, Supplier) 重载（用反射版）
ClickListener 的 pressed/pressedPointer/cancelled 是 private，取消一次点击要用 cancel()（原版直接改字段）

自然岩壁（照搬 world/blocks/Rock + StaticWall）：
TextureRegion.split 的下标顺序 libgdx 与 arc 相反：libgdx 是 [row][col]（row 0 = 图像顶行），
     原版 StaticWall.draw 写 split[x%2][1-y%2] 照抄会摆错四象限；正确写法是 split[1 - y%2][x%2]
     （已用 Python 对全部 *-large.png 做「按映射拼回原图」逐张验证）
小地图取色要用 icon(Cicon.full) 而不是 region：Rock 的贴图命名是 name1..nameN，裸名 region 会回退成 blank
菜单显隐应由状态驱动（对应原版 UI 的 menuGroup.visible(() -> state.is(State.menu))），
     否则「胜负判定 → Control.menu()」只改状态、没人调 show()，会停在空白清屏色上

特效系统（照搬 content/Fx + entities/Effects，2026/9 第三批）：
arc 的 Lines/Fill/Drawf 已补在 com.phoenix.game.core，用 libgdx **ShapeRenderer** 实现（不是原版的共享 Mesh）：
     - SpriteBatch 只能画带贴图的四边形，画不了任意三角形，所以几何原语走 ShapeRenderer
     - 会话连续画几何时只开一次（setAutoShapeType(true) + set(ShapeType)，只在 Filled/Line 切换时 flush）；
       **Draw.rect 画精灵前会先 Drawf.end() 收尾**，保证「精灵→几何→精灵」的顺序严格成立（只靠"几何前 flush 批次"是不够的：
       若顺序是 几何A → 精灵B → 几何C，B 会被压到 A、C 之后）；Effects.render() 末尾也会收尾
     - 线宽不用 glLineWidth（现代 GL 核心只有 1px），线段一律画成有宽度的四边形（ShapeRenderer.rectLine），stroke 任意粗细都真实生效
     - ImmediateModeRenderer20 **没有容量检查**，顶点写满直接数组越界；Drawf.begin() 里留 1024 顶点余量，接近上限先 shapes.flush()
       （最大的图元是 96 段的圆 = 576 顶点，余量必须大于它）
arc 的小写颜色常量 libgdx **没有**：Color.gray/white/lightGray/darkGray/black/orange/yellow/royal/lime
     一律换成大写 Color.GRAY/WHITE/LIGHT_GRAY/DARK_GRAY/BLACK/ORANGE/YELLOW/ROYAL/LIME
Draw.color(a, b, c, s) 三段渐变重载已补（s<0.5 走 a→b，否则 b→c）；libgdx 原本只有两色版
EffectContainer 已补 fout()/finpow()/fslope()/scaled(lifetime, renderer)（scaled 的语义是"临时换 lifetime 再画一次"，回调收到的就是它自己）
Mathf 已补 angle(x,y)（atan2(y,x) 转角度）与 randomSeedRange(seed, range)；缓动补在 math/Interp（pow 族，linear/pow2~pow5 的 in/out/inOut）
特效的 e.rotation 语义**不统一**，接线时别乱传：placeBlock/breakBlock/tapBlock/door*/healBlock*/overdriveBlockFull 当**方块边长倍数**
     （原版传 block.size），overdriveWave/healWaveMend/shieldBreak/spawnShockwave 当**半径**，
     artilleryTrail/missileTrail 当**初始半径**，hit*/despawn/vtolHover 当**方向角**
原版大部分炮口/命中特效挂在 BulletType 上（Turret 会退回 peekAmmo(tile).shootEffect）；
     BulletType 的 shootEffect/smokeEffect 字段已于 2026/9 第四批补齐，见下方「子弹/炮塔系统」小节
未移植系统的替代：GroundEffect（unitDrop/unitLand/unitPickup/landShock/shellEject*）退化成普通 Effect；
     fire 里的 renderer.lights.add 跳过；freezing/melting/wet/oily 用原版液体的字面色值（Liquids 未移植）
贴图依赖：circle/white/blank/casing 四个区域在 sprites.atlas 里都有；atlas 里**没有** tri/line/ring/square 这类几何贴图，
     所以几何只能靠 ShapeRenderer 画，不要再想着"用贴图凑"
后台验证设施新增 `PHOENIX_LOGTEST` 模式 26（特效画廊：反射收集 Fx 全部 Effect，铺成 12 列网格每 25 帧重放一遍，
     配 `PHOENIX_SHOT` 抓帧看；`Fx.none` 因 lifetime<=0 会被跳过）

子弹/炮塔系统（照搬 entities/bullet/* + content/Bullets，2026/9 第四批）：
BulletType 补齐 shootEffect/smokeEffect（默认 Fx.shootSmall / Fx.shootSmallSmoke），Bullet 补 fout()/fslope()（对应原版 Scaled）
Turret.effects() 照搬原版退回逻辑：**炮塔自身为 Fx.none 时用弹药的**，否则用炮塔自己的
     → 关键坑：原先 Blocks.turret() 辅助方法里硬编码了 t.shootEffect = Fx.shootSmall，会**整体屏蔽退回机制**，已删掉；
       炮塔级只保留原版确实覆盖的那几个（lancer 的 lancerLaserShoot/ShootSmoke、arc 的 lightningShoot、meltdown 的 shootBigSmoke2）
BasicBulletType 新增 bulletSprite 参数（"bullet"/"shell"/"missile"，去找 "<name>-back" 与 "<name>"；这三个贴图 atlas 里都有），
     backRegion/frontRegion/load/drawRegions 提升为 protected 供子类复用
新增四个子类（结构照搬原版，物理属性按本项目现状调整）：
     ArtilleryBulletType  飞行中周期撒 artilleryTrail（间隔 3 + fslope*2），绘制尺寸随 fslope 先大后小
     FlakBulletType       近炸引信：每 6 tick 检测 30 半径内敌人，命中就 b.setData(0) 并在 5 tick 后强制到期（data 当状态位）
     MissileBulletType    默认 homingPower=7；随机撒 missileTrail；weaveMag>0 时叠加蛇形（相位用 b.id*4422）
     BombBulletType       低速高阻抛体（speed 0.7 / drag 0.05 / keepVelocity=false）
     ⚠原版 Artillery/Bomb 构造器设 collides=collidesTiles=collidesAir=false，依赖抛物线**落地检测**；
       本项目没有该检测，照搬会让炮弹直接穿过目标 → 这两个子类**保持碰撞开启**（刻意偏离，已在类注释写明）
Bullets 按原版对应弹药配炮口特效：standardCopper/flakBullet→shootSmall(+shootSmallSmoke)、flameBullet→shootSmallFlame、
     eradicationBullet→shootBig(+shootBigSmoke)、bombExplosive→none（原版炸弹由机腹投下，不播炮口特效）
     artilleryBullet 改用 ArtilleryBulletType、flakBullet 改用 FlakBulletType、bombExplosive 改用 BombBulletType；
     swarmer 的弹药由 chaosBullet 换成新增的 missileBullet（MissileBulletType）——原版 swarmer 就是导弹
炮塔块的伤害/射程/装填数值一律不动（沿用既有刻意简化），只让特效与子弹行为对齐原版
后台验证设施新增 `PHOENIX_LOGTEST` 模式 27（一排弹药各异的炮塔 + 周期放敌人持续开火；抓帧应能看到三种形态不同的
     炮口特效：shootSmallFlame 橙色火焰三角、shootSmall 白环、shootSmallSmoke 灰烟团）
     ⚠`PHOENIX_LOGTEST` 的格式是 **`<结束帧>:<模式>`**（不是 模式:帧），写反会静默跑成默认的传送带测试（模式 1）

激光炮塔光束（照搬 Bullets.lancerLaser/meltdownLaser，2026/9 第五批）：
新增 `Damage.collideLine`（线段命中）：原版用 `Geometry.raycastRect` 求"线段与单位包围盒"的精确交点，
     这里退化成「点到线段距离 &lt; 单位半径」判定（先用线段包围盒粗筛）；建筑部分走 `world.raycastEach` 逐格，
     `large=true` 时额外查四邻（meltdown 的粗光束）
补 `Mathf.curve(f, min, max)` 并**夹到 [0,1]**：lancerLaser 用它算"前 20% 生命周期把光束伸长到全长"，
     不夹取的话 fin→1 时 f=5、光束会长成 5 倍
`Bullets.lancerLaser`：`init` 里 `collideLine` 做瞬时命中；draw 是 3 层颜色 × 4 档粗细/长度叠加（浅蓝外缘 + 白芯）
`Bullets.meltdownLaser`：`update` 里每 5 tick 再 `collideLine`（持续伤害）+ 每帧震屏；draw 是 4 层带脉动（橙外缘 + 白芯）
     ⚠原版 hit 里会 `Fire.create` 点燃（0.4 概率）→ 已在第六批接入 Fire 系统后补上
Blocks 里 `lancer → lancerLaser`、`meltdown → meltdownLaser`（炮塔数值不动）
`Renderer.drawBullets` 末尾补 `Drawf.end()`：激光弹是**纯几何**，不收尾会让后面的 batch 精灵（电力线/血条）
     先于几何落盘，绘制顺序错乱
模式 27 同时用来验证激光束，踩到的三个坑：
     ①建筑不能建在液体上（`placeBlock` 静默失败）→ 布局前要先把水面填成陆地（`fillLiquidArea`）
     ②激光炮塔射程只有 155/190，按"玩家位置 + 偏移"估敌人坐标会差几百单位 → 要用**炮塔实体的实际坐标**定位
     ③meltdown 耗电 1.5/帧，一块太阳能板（0.06）供不起 → `cons.valid()` 不通过、炮塔根本不开火；
       测试里改成直接创建它的激光弹来验证绘制（炮塔侧接线与 lancer 同源）

Fire 燃烧系统（照搬 entities/effect/Fire，2026/9 第六批）：
新增 `entities/effect/Fire`：每格一个火，`static Array<Fire> all`（对应原版 fireGroup）+ `Map<Integer,Fire>`（按瓦片 pos 索引）。
     火焰本身**不绘制**（原版同理），靠 update 里随机喷 `Fx.fire`(0.1/tick) / `Fx.fireSmoke`(0.05/tick) 表现；
     可燃性 > 1 时按概率蔓延到随机邻格（Geometry.d4），并有 0.07 概率甩出 `Bullets.fireball`；
     周期性烧建筑 0.4 血 + 给附近**地面**单位（`!isFlying && !isImmune(burning)`）挂 5 秒 burning；
     没有可燃物（`!damage && flammability<=0`）时 `time += delta*8`，即 8 倍速烧完 ≈ 被浇灭
`Block.getFlammability(Tile)`：无物品库存时返回**地板液体**的可燃性，有库存时累加 `item.flammability*amount`
     （有液体储量再按 1/3 折算）。水地板可燃性 0 → 火落在水上自动快速熄灭
     ⚠原版读 `tile.floor().liquidDrop.flammability`（每个液体地板挂一个 Liquid 内容），本项目没有液体地板内容，
       改为给 `Floor` 加一个 `flammability` 字段直接标在地板上
`Damage.createIncend` 从"只喷 Fx.fire 粒子"改成**真的在范围内随机取瓦片点火**（对应原版），
     于是 `BulletType.incendChance/incendAmount`（flameBullet 0.3/2）与 `Damage.dynamicExplosion` 的
     flammability 分支现在都会产生真实火势
新增 `Damage.damageUnits(team, x, y, size, damage, predicate, acceptor)`（原版同名方法的简化）：
     圆形范围内按条件打伤害并回调；`team==null` 时不分敌我（火系统就是这么用的）
新增 `Bullets.fireball`：pierce + `collides/collidesTiles=false` + `hitTiles=false` + drag 0.03，
     `init` 里把初速重设成 `0.6+random(2)`；update 里 0.04 概率沿路径点火 + 喷 fireballsmoke/ballfire；
     draw 是 `Fill.circle` 从 lightFlame→darkFlame→gray 随 fin 渐变
`Logic.update` 在 `world.updateTiles()` 之后调 `Fire.updateAll()`（对应原版 tileGroup 之后的 fireGroup.update）；
`Control` 三处清场（loadWorld / 读档 / 回菜单）都补了 `Fire.clear()`
刻意偏离（已在 Fire 类注释写明）：
     ①**不做联机同步**：原版 `Fire.create` 在客机直接 return（服务端权威 + SyncTrait）。本项目建筑逻辑两端都跑仿真
       （子弹也是两端各自 Bullet.create），所以火两端各自生成；建筑掉血走 `TileEntity.handleDamage`，
       它在客机是 no-op，血量仍由服务端广播，不会两端打架
     ②没有液体水洼（Puddle）系统 → 原版"读脚下水洼可燃性让火烧更久"跳过
     ③音效无距离衰减（原版 `Sounds.fire.at(this)`）→ 改成只在镜头 600 单位内才播
     ④不落存档（原版实现 SaveTrait）→ 读档后火不恢复
后台验证设施新增 `PHOENIX_LOGTEST` 模式 28：一排装满 pyratite（可燃性 1.4）的容器 + 水域/草地两块对照火点。
     抓帧应看到橙黄火点 + 灰烟团在容器上蔓延；日志 `DBG fire count=` 由 5 涨到 16~22（蔓延生效），
     `water=false grass=false`（无燃料的火已熄灭）、`unitBurning=true` 且 unitHp 下降（燃烧状态生效），
     `waterFloor=water grassFloor=grass` 用来确认对照组地板真的换成了水（否则测试是空跑）
